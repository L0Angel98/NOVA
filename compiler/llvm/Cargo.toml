[package]
name = "nova-llvm"
version = "0.1.3"
edition = "2021"

[dependencies]
anyhow = "1.0"
inkwell = { version = "0.4.0", default-features = false, features = ["llvm17-0-no-llvm-linking"] }
serde_json = "1.0"

